<?xml version="1.0" encoding='UTF-8'?>
<odoo>
    <!--
    cron not needed anymore but will serve as reference    
        <data noupdate="1">
        <record id="cron_jwt_auth" model="ir.cron">
            <field name="name">JWT Auth Cron</field>
            <field name="model_id" ref="model_imerex_erp_jwt_auth"/>
            <field name="state">code</field>
            <field name="code">model.jwt_authentication()</field>
            <field name="user_id" ref="base.user_root"/>
            <field name='interval_number'>11</field>
            <field name='interval_type'>months</field>
            <field name="numbercall">-1</field>
            <field name="doall" eval="False"/>
        </record>
    </data> -->
</odoo>